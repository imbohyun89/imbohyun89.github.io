<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wt.mapper.UserMapper">
	<resultMap type="user" id="userResult">
		<result column="id" property="id"/>
		<result column="pwd" property="pwd"/>
		<result column="email" property="email"/>
		<result column="gender" property="gender"/>
		<result column="birth" property="birth"/>
		<result column="admin" property="admin"/>
	</resultMap>
	
	<select id="select" parameterType="String" resultMap="userResult">
		SELECT * FROM WT_USER WHERE ID=#{obj}
	</select>
	<select id="selectall" resultType="user">
		SELECT * FROM WT_USER
	</select>
	<insert id="insert" parameterType="user">
		INSERT INTO WT_USER VALUES(#{id},#{pwd},#{email},#{gender},#{birth},#{admin})
	</insert>
	<update id="update" parameterType="user">
		UPDATE WT_USER SET PWD=#{pwd},NAME=#{name},EMAIL=#{email},GENDER=#{gender},BIRTH=#{birth},ADMIN=#{admin} WHERE ID=#{id}
	</update>
	<delete id="delete" parameterType="String">
		DELETE FROM WT_USER WHERE ID=#{obj}
	</delete>
	
</mapper>






